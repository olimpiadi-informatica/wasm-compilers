name: Build artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - ci
  pull_request:
    branches:
      - main

jobs:
  build-artifacts:
    name: Build artifacts
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
    steps:
      - name: Install Deps
        run: |
          sudo apt update
          sudo apt install -yy build-essential clang brotli binaryen ninja-build gcc-multilib
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Install wasmtime
        run: cargo install wasmtime-cli
      - name: Test wasmtime installation
        run: which wasmtime
      - name: Install wasm32-wasip1-threads Rust target
        run: rustup target add wasm32-wasip1-threads
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Build host tools
        run: make build/llvm-host.BUILT
      - name: Build Rust
        run: make build/rust.BUILT
      - name: Build C++ compiler
        run: make build/llvm.BUILT
      - name: Build Python
        run: make build/python.BUILT build/ty.BUILT
      - name: Finalize build
        run: make
      - name: Upload built compilers
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: compilers
          path: build/output/*.tar.br
          compression-level: 0
